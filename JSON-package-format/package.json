{
  "name": "cudl-schema-package-json",
  "version": "1.0.0-alpha.4",
  "description": "Schemas for CUDL's package data in JSON format",
  "main": "build/manifest.json",
  "files": [
    "examples/**/*.json",
    "examples/**/*.json5",
    "schemas/*.json",
    "tests/**/*.json",
    "tests/**/*.json5"
  ],
  "directories": {
    "schemas": "schemas",
    "examples": "examples",
    "tests": "tests"
  },
  "scripts": {
    "generate-manifest": "mkdir -p build && find examples tests schemas -name '*.json' -or -name '*.json5' | jq --raw-input '{files: [inputs]}' > build/manifest.json",
    "prepack": "npm run generate-manifest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://bitbucket.org/CUDL/cudl-package-schemas.git"
  },
  "license": "UNLICENSED",
  "homepage": "https://bitbucket.org/CUDL/cudl-package-schemas"
}
